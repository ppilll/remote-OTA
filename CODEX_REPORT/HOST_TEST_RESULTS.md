# Executed host test results

Execution date: 2026-09-02 (Asia/Shanghai workspace date; application JSON logs used UTC)

## Environment actually used

```text
Python 3.8.5 (MSC v.1916 64 bit)
Windows-10-10.0.22621-SP0
```

Required VM environment remains Ubuntu 20.04 with Python 3.8.10. Therefore these results are Codex-host evidence and must be repeated on that VM before applying the R2 `HOST VERIFIED` label.

## Automated suite

Executed:

```text
.venv\Scripts\python.exe -m pytest -q
```

Observed final result:

```text
................................                                         [100%]
32 passed in 0.94s
```

The suite covers:

- exact manifest schema and configured R2 values;
- independent source-file SHA256 and byte-size comparison;
- full download status, headers, exact bytes, and digest;
- start/end, middle, final-byte, suffix, and open-ended single ranges;
- malformed number/unit/syntax rejection;
- comma-separated multi-range rejection;
- bounded rejection of overlong bounds and headers;
- beyond-EOF, inverted, and zero-suffix 416 behavior with `Content-Range: bytes */<size>`;
- configured artifact removal causing manifest 503 and artifact 404, with fixture restoration;
- injected artifact read denial causing a defined 500 without a client traceback and with an internal structured error reason;
- valid report 204 and structured report fields;
- missing/empty `device_id`, empty version, unknown status, invalid timestamp, and timezone-free timestamp rejection;
- malformed JSON 400;
- structured request, artifact, report, and validation logging;
- absence of an injected secret marker from captured logs;
- 20 consistent manifest/artifact requests issued through a 10-worker concurrency pool.

## Real-listener smoke

Executed a local listener:

```text
.venv\Scripts\python.exe -m uvicorn app.main:app --host 127.0.0.1 --port 8000
```

Then executed:

```text
.venv\Scripts\python.exe scripts\host_smoke.py --base-url http://127.0.0.1:8000
```

Observed:

```text
HOST SMOKE PASSED
manifest version: 1.1.0
artifact size: 33554432
artifact sha256: 4489164df7478188c3fd968f2c8d77a6e2a0522ae051972f588495cf9e118823
```

The captured server output contained JSON events for manifest HTTP 200, full artifact HTTP 200, partial artifact HTTP 206, and device report HTTP 204, including client IP, method/path, response code, artifact access type, and allowed report identity/status fields.

## Independent artifact observation

The artifact was generated by `scripts/generate_dummy_artifact.py` and then checked with PowerShell `Get-FileHash -Algorithm SHA256` and `Get-Item`.

Observed:

```text
size: 33554432
sha256: 4489164DF7478188C3FD968F2C8D77A6E2A0522AE051972F588495CF9E118823
```

This file is deterministic dummy test data, not a valid RAUC bundle. SHA256 is integrity metadata, not authenticity evidence.
